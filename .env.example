# Optional: the game works without a database using on-device storage.
# Add a PostgreSQL connection string to sync profiles across devices.
# Neon:      postgresql://USER:PASSWORD@HOST/DATABASE?sslmode=require
# Supabase:  postgresql://USER:PASSWORD@HOST:6543/postgres?sslmode=require
DATABASE_URL=
